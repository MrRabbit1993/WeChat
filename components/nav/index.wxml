<view class="container">
    <image class="icon" src="{{latest?disLeftSrc:leftSrc}}" bind:tap="leftFn" />
    <text class="title">{{title}}</text>
    <image class="icon" src="{{first?disRightSrc:rightSrc}}" bind:tap="rightFn" />
</view>